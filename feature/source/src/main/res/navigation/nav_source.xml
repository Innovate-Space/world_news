<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_source"
    app:startDestination="@id/sourceFragment">

    <fragment
        android:id="@+id/sourceFragment"
        android:name="co.innovatespace.source.SourceFragment"
        android:label="@string/sources"
        tools:layout="@layout/fragment_source">
        <argument
            android:name="ShowSidebar"
            android:defaultValue="true" />
        <argument
            android:name="ShowBottomNav"
            android:defaultValue="true" />
        <deepLink app:uri="app://innovatespace.co/sources" />
    </fragment>
</navigation>